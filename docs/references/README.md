# Reference sheets

Agreed simplified-schematic references for each line, used as the visual target for the SVGs.

- `machining_reference.png` — machining line reference layout.
- `testing_reference.png` — testing line reference layout.

The SVGs trace these (simplified, recognizable geometry) per TZ — they are not redrawn from
the raw CAD sheet.
