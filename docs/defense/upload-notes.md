# Talent Track upload — notes

- Repository: https://github.com/richizam/RKTR
- Entry point: open `index.html` in a browser (no build step, no server required).
- Demo path: see `docs/demo-script.md`.
- Run for the defense from a local static server if the browser blocks `fetch()` of the SVGs:
  `python -m http.server 8000` then open `http://localhost:8000/`.

## What to hand in
- Link to this repo (history shows the full April→June development).
- Deck + project description + video pitch.
- Demo screenshots are in the repo root (`demo-*.png`).
